# goFarmacia — Variables de entorno
# Copia este archivo como .env y ajusta los valores

# Conexión a PostgreSQL
DATABASE_URL=postgresql://luna:tu_password_seguro@localhost:5432/farmacia_db?sslmode=disable

# Clave secreta para firmar tokens JWT (mínimo 32 caracteres, cambia esto en producción)
JWT_SECRET_KEY=cambia_esto_por_una_clave_segura_de_32_caracteres_minimo
